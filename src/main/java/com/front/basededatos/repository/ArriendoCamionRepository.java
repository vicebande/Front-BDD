package com.front.basededatos.repository;

import com.front.basededatos.model.ArriendoCamion;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ArriendoCamionRepository extends JpaRepository<ArriendoCamion, Long> {
}
